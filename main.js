// variable  it is a name storage for a value/data
// we use let keyword to declare a variable in javascript i,e
let messsage;
message = 'hello';
alert(message)


// declaring multiple variables in one line
//i.e 
let age=25,message='helo', name='jonte';

// declaring variables in a multiline
let user = 'malo';
let age = 25;
let message = 'hello';

// declaring varibales using the comma first method.
let user = 'john'
, age = 25
,message = 'hello';


// using the keyword var to declare a variable
// we can assisgn any value to a variable and change it as many times as we want
let message;
message = 'hello';
message = 'world';
alert(message); // explain this line of code
// the output of the code above is world, because the value of the message variable is changed from hello to world.
// what does alert do?
// alert is a function that shows a message to the user in a dialog box.
// when the value is changed , the old data is removed and replaced with the new data.
// the value of the variable can be changed as many times as possible.

// we can also declare 2 variables copy data from one to the other
let hello = 'hello world';
let message;
message = hello;
alert(hello); // explain this line of code
// the output of the code above is hello world, because the value of the hello variable is copied to the message variable.
alert(message); // explain this line of code
// the output of the code above is hello world, because the value of the hello variable is copied to the message variable.


// task 1, declare 2 variables: admin and name.
let admin;
let name;
let name= 'john';
let admin = name;
alert(admin);

// task2, create a variable with the name of our planet. how would you name such a variable?
let ourPlanetEarth = 'earth';
alert(ourPlanetEarth);

// task 3, create a variable to store the name of a current visitor to a website. how would you name that variable?
let currentVisitor = 'john';
alert(currentVisitor);

// task 4, is it possible to change the value of a constant once it has been declared?
// no, it is not possible to change the value of a constant once it has been declared.  git


